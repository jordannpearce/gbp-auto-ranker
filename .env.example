# Operator password for /dashboard
DASHBOARD_PASSWORD=gbp-admin

# Used to sign the dashboard session cookie. Change this in production.
DASHBOARD_SECRET=change-this-dashboard-secret

# Persist customer records. On Railway, mount a volume and point this at it.
DATA_DIR=./data
